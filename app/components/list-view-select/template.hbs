{{#if this.setListViews.isIdle}}
  <InputSelect
    @value={{this.selectedValue}}
    @selectOptions={{this.listViewSelectOptions}}
    @required={{true}}
    @class="list-view"
    @valueChanged={{this.selectionChanged}}
  />
{{/if}}