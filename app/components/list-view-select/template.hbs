{{#if setListViews.isIdle}}
  <InputSelect
    @value={{this.selectedValue}}
    @selectOptions={{this.listViewSelectOptions}}
    @required={{true}}
    @class="list-view"
    @valueChanged={{action 'selectionChanged'}} />
{{/if}}