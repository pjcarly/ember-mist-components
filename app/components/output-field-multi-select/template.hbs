{{#if loadSelectOptions.isIdle}}
  <OutputMultiSelect
    @value={{this.value}}
    @selectOptions={{this.selectOptions}}
    @prefix={{@prefix}}
    @suffix={{@suffix}}
    @class={{@outputClass}}
    @options={{this.outputOptions}} />
{{else}}
  <Loading />
{{/if}}