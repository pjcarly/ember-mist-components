<button type="button" class="btn btn-primary" {{on "click" (perform this.generatePdf)}} ...attributes>
  {{#if hasBlock}}
    {{yield this.generatePdf}}
  {{else}}
    {{#if this.generatePdf.isIdle}}
      Generate
    {{else}}
      Generating <Loading />
    {{/if}}
  {{/if}}
</button>